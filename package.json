{
  "scripts": {
    "dev": "parcel src/index.html",
    "prebuild": "rimraf docs",
    "build": "parcel build src/index.html -d docs --public-url ./"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "rimraf": "^3.0.2",
    "sass": "^1.32.12"
  },
  "dependencies": {
    "ress": "^4.0.0"
  }
}
